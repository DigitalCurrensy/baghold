# Architecture

`bag.py` is the hold kernel. Wave 0 names it. Wave 0 does not execute it as a bag verdict.

Declared caches: SYN-HOLD-MTP and SYN-HOLD-MHP. PDS LOLA, SLDEM2015, and NAC_DTM_MARIUSPIT01 stay unfetched.

Fail-closed: pinch first, then floor void, then drop, then floor slope. Equality sits.

Parent frame is IAU ME/PA. Wagner 2022 vs Haruyama discovery is named mismatch, not a score.
